// Renvoie le carré d'un nombre
//Complétez le programmecarre.js ci-dessous pour
//que la fonctioncarre() renvoie le carré d'un nombre passé en paramètre.
function carre(x) {
    return (x*x);
}


console.log(carre(0)); // Doit afficher 0
console.log(carre(2)); // Doit afficher 4
console.log(carre(5)); // Doit afficher 25

for(i=0 ; i<=10 ; i++){
  console.log(carre(i));
}
